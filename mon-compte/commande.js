'use strict';
const money=cents=>new Intl.NumberFormat('fr-FR',{style:'currency',currency:'EUR'}).format(cents/100);
async function requireManager(){const response=await fetch('../api/auth/session/',{headers:{Accept:'application/json'},credentials:'same-origin'});if(response.status===401){location.replace('../compte/?session=expired');return false}const data=await response.json().catch(()=>({}));if(!response.ok||data.user?.role!=='manager'){location.replace('./');return false}document.querySelector('#header-user').textContent=data.user.firstName+' '+data.user.lastName;return true}
async function render(){const id=new URLSearchParams(location.search).get('id');if(!id){location.replace('./#subscription');return}const response=await fetch('../api/orders/?id='+encodeURIComponent(id),{headers:{Accept:'application/json'},credentials:'same-origin'}),order=await response.json().catch(()=>({}));if(!response.ok)throw new Error(order.message||'Commande introuvable.');document.querySelector('#order-summary').innerHTML='<div class="summary-head"><span>Désignation</span><span>Quantité</span><span>Montant HT/mois</span></div>'+order.items.map(item=>'<div class="summary-row"><span>'+item.description+'</span><span>'+item.quantity+'</span><strong>'+(item.totalCents===0?'Inclus':money(item.totalCents))+'</strong></div>').join('');document.querySelector('#checkout-total').textContent=money(order.totalCents)+' HT/mois'}
document.querySelector('#validate-order').addEventListener('change',event=>document.querySelector('#payment-button').disabled=!event.currentTarget.checked);
document.querySelector('#payment-button').addEventListener('click',()=>{const message=document.querySelector('#payment-message');message.textContent='Votre commande est prête. Le prestataire de paiement sécurisé doit maintenant être raccordé avant de pouvoir encaisser.';message.classList.add('visible')});
document.querySelector('#logout').addEventListener('click',async()=>{try{await fetch('../api/auth/logout/',{method:'POST',headers:{Accept:'application/json'},credentials:'same-origin'})}finally{location.replace('../compte/')}});
requireManager().then(ok=>{if(ok)return render()}).catch(error=>{const message=document.querySelector('#payment-message');message.textContent=error.message;message.classList.add('visible')});
